#include<iostream>
#include<vector>
using namespace std;

int main(){
    //creation
    cout<<"Vector name arr"<<endl;
    vector<int>arr;
    // cout<<arr.max_size()<<endl;
    // arr.push_back(10);
    // arr.push_back(20);
    // arr.push_back(30);
    // arr.push_back(40);
    // arr.clear();

    // arr.insert(arr.begin(),2,50);
    // cout<<*(arr.begin())<<endl;
    // cout<<arr[1]<<endl;
    // cout<<arr[2]<<endl;

    // cout<<arr.size()<<endl;

    // arr.erase(arr.begin(),arr.end());
    // cout<<arr.size()<<endl;

    // arr[0]=100;
    // cout<<arr[0]<<endl;
    // cout<<arr.at(0)<<endl;

    // cout<<"capacity "<<arr.capacity()<<endl;

    // arr.push_back(50);

    // cout<<"Size "<<arr.size()<<endl;
    // cout<<"capacity "<<arr.capacity()<<endl;

    cout<<endl<<endl;

    // cout<<"Size "<<arr.size()<<endl;
    // arr.pop_back();
    // cout<<"Size "<<arr.size()<<endl;
    // cout<<arr.front()<<endl;
    // cout<<arr.back()<<endl;
    // if(arr.empty()==true){
    //     cout<<"Empty"<<endl;
    // }else{
    //     cout<<"Not Empty"<<endl;
    // }



    // cout<<"Vector name marks"<<endl;
    
    // vector<int>marks;
    // cout<<"capacity "<<marks.capacity()<<endl;
    // marks.reserve(10);
    // cout<<"capacity "<<marks.capacity()<<endl;


    // vector<int>arr1(5);

    // vector<int>arr2(5,10);
    // cout<<*(arr2.begin())<<endl;


    /*Swaping two vector*/

   /*
    vector<int> first;
    vector<int> second;

    first.push_back(10);
    first.push_back(11);
    first.push_back(12);
    first.push_back(13);
    

    //traversing the vector using iterator

    // creating the iterator
    vector<int>::iterator it = first.begin();

    while(it != first.end()){
        cout<<*it<<" ";
        it++;
    }
    */


   /* 
    second.push_back(100);
    second.push_back(200);
    second.push_back(300);
    second.push_back(400);

    first.swap(second);

    cout<<first[0]<<" "<<first[1]<<" "<<first[2]<<" "<<first[3]<<endl;

    for(int i : first){
        cout<<i<<" ";
    }
    cout<<endl;
    for(int i : second){
        cout<<i<<" ";
    } 
    */




   /* 2D VECTOR*/

    /*2D array created with 5 rows with 4 column and with initial value 0*/
//    vector< vector<int> > arr(5, vector<int> (4,0));
//    int totalRows = arr.size();
//    int totalColumn = arr[0].size();

//    cout<<totalRows<<endl;
//    cout<<totalColumn<<endl;

    // JACKED ARRAY

    // vector< vector<int>> arr1(4);
    // arr1[0] = vector<int>(4);
    // arr2[1] = vector<int>(2);
    // arr2[2] = vector<int>(5);
    // arr2[3] = vector<int>(3);

    // int totalRowCount = arr1.size();
  
    // int totalColumnCount = arr1[i].size();



    return 0;
}